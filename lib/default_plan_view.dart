import 'package:flutter/material.dart';

class DefaultPlanView extends StatelessWidget {
  const DefaultPlanView({Key? key}) : super(key: key);
  static const routerName = "default_plan_view";
  @override
  Widget build(BuildContext context) {
    return const Text("DefaultPlanView");
  }
}
